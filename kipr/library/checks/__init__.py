"""Checks stage: KLC-style deterministic rules plus (optionally) KiCad's official KLC checkers,
run on the render stage's OUT. Writes ``review.json`` and ``review.md``. Entry point:
:func:`main.run`."""
